# spring:
#   application:
#     name: USER-DEFAULT

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/microservice_assignment
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
eureka:
  instance:
    hostname: localhost
